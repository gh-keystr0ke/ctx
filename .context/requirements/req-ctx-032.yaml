id: REQ-CTX-032
type: requirement
status: active
statement: For impact, explain, and find queries, CLI text output must section multiple matches as `[i/N]`, while JSON output must return a query and matches array rather than a combined result object.
implementation:
  - symbol: ctx_cli.impact
  - symbol: ctx_cli.explain
  - symbol: ctx_cli.find
tests:
  - symbol: crates.ctx-cli.tests.e2e.mixed_python_rust_and_go_repository_indexes_and_reviews_through_one_registry

