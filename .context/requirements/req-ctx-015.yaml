id: REQ-CTX-015
type: requirement
status: active
statement: Malformed agent JSON must fail rather than be interpreted heuristically, and subprocess arguments must not use shell-string interpolation.
implementation:
  - symbol: ctx_adapters.agent_contract.extract_json_object
  - symbol: ctx_adapters.claude_code.SubprocessTransport.AgentTransport.run
tests:
  - symbol: ctx_adapters.agent_contract.tests.malformed_output_is_rejected_not_guessed_at
  - symbol: ctx_adapters.claude_code.tests.subprocess_transport_invokes_claude_p_safe_mode_and_returns_stdout

