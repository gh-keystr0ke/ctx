id: INV-CTX-084
type: invariant
status: active
statement: Schema diffs must never infer column renames; absent and newly named columns are reported independently unless the source explicitly declares a rename.
implementation:
  - symbol: ctx_core.schema.diff_schema_tables
tests:
  - symbol: ctx_core.schema.tests.diff_does_not_guess_a_rename_from_name_similarity
  - symbol: ctx_core.schema.tests.diff_respects_an_explicit_rename_declaration
