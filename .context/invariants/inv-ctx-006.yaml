id: INV-CTX-006
type: invariant
status: active
statement: A cross-file symbol move reported by Git as a delete/add pair must be merged into one silent Rename so findings for its mapped invariant or requirement are not duplicated.
implementation:
- symbol: ctx_core.review.merge_cross_file_moves
tests:
- symbol: ctx_core.review.tests.cross_file_move_merges_into_one_silent_rename
