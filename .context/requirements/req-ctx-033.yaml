id: REQ-CTX-033
type: requirement
status: active
statement: Git ingestion must idempotently upsert artifacts and resolve text references against every artifact already known to the repository, including artifacts stored by prior runs.
implementation:
  - symbol: ctx_app.ingest.GitIngestRunner.run
tests:
  - symbol: crates.ctx-cli.tests.e2e.ingest_git_reads_commits_and_branches_and_links_a_referenced_ticket
